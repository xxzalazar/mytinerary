/*import React, { Component, useState, useEffect } from 'react';
import axios from 'axios';

const itinerariess = (props) => {
  const searchId = props.match.params.id;
  useEffect(() => {
    axios.get(`http://127.0.0.1:4000/api/Itineraries/${searchId}`);
  });

  const [itinerariesList, list] = useState(response.data.it);
  console.log(searchId);
  return <div>dsdss</div>;
};

export default itinerariess;*/
